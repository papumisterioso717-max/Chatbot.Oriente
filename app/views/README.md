La fase 2 sirve `chat.html` desde `/`, con estilos y comportamiento en `static/`.
La vista consume la API existente; los textos y opciones conversacionales siguen
procediendo exclusivamente del YAML. No requiere dependencias adicionales.
